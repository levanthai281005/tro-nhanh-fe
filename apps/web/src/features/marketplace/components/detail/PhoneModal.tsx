'use client';

export interface PhoneModalProps {
  open: boolean;
  phone: string;
  redirectPath: string;
  viewerId?: string;
  onClose: () => void;
}

export function PhoneModal() {
  return null;
}
