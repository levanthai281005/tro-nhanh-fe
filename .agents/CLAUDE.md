@AGENTS.md

Đọc thêm toàn bộ quy tắc trong `rules/`, tài liệu nghiệp vụ trong `business/`, quy trình công
việc trong `tasks/` và kỹ năng chuyên biệt trong `skills/` trước khi thực hiện thay đổi.

Mỗi thư mục có `README.md` làm mục lục — dùng nó để đọc đúng file cần, thay vì đọc tất cả.
