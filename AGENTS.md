# Trọ Nhanh Frontend — Hướng dẫn cho agent

Toàn bộ quy tắc làm việc nằm trong thư mục `.agents/`. Đọc theo thứ tự:

1. `.agents/AGENTS.md` — nguyên tắc chung và bản đồ tài liệu
2. `.agents/rules/` — quy ước viết code và đặt tên (luôn đọc trước khi code)
3. `.agents/business/README.md` — mục lục nghiệp vụ, đọc đúng file cần
4. `.agents/tasks/` — quy trình cho từng loại công việc
5. `.agents/skills/` — kỹ năng chuyên biệt kèm bảng tra

Repo `prototype/` nằm ở thư mục cha là bản tham chiếu **chỉ đọc** — không bao giờ sửa.

Trước khi bàn giao: chạy `pnpm lint`, `pnpm typecheck`, `pnpm build` và đối chiếu
`.agents/tasks/PRE_HANDOFF_CHECKLIST.md`.
