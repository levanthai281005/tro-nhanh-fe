import type { ReactNode } from 'react';

export interface SearchFieldProps {
  icon: ReactNode;
  label: string;
  placeholder: string;
}

export function SearchField(_props: SearchFieldProps) {
  return null;
}
